# Test Suite - Dipendenze Open Source
# Tutte MIT / BSD / Apache 2.0

pytest>=7.4.0           # Framework test principale (MIT)
pytest-mock>=3.12.0     # Mocking integrato con pytest (MIT)
pytest-xdist>=3.5.0     # Esecuzione test in parallelo (MIT)
pytest-timeout>=2.2.0   # Timeout per ogni test (MIT)
coverage>=7.4.0         # Analisi copertura codice (Apache 2.0)
pytest-cov>=4.1.0       # Integrazione pytest + coverage (MIT)
